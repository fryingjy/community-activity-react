{
  "name": "community-activity-lite",
  "version": "5.17.3",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js tests/simulator/*.test.js",
    "check:syntax": "node --check background.js && node --check liteScanner.js && node --check domScan.js && node --check sidepanel.js && node --check rosterSources.js && node --check observedAuthors.js && node --check memberArchive.js && node --check graphqlContracts.js && node --check diagnostics.js",
    "check": "npm run check:syntax && npm test"
  }
}
